# User Cosmetics schema
Read `.agents/skills/project-schema-review/SKILL.md` before migrations/index changes. Preserve catalog ownership/selection integrity, existing rows, FK/unique/null behavior, rollback/recovery, and query indexes. Never perform destructive production DB work as part of development.

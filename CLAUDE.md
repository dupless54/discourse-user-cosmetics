@AGENTS.md

# Claude Code adapter
Read only nearest local `AGENTS.md` files for the current task. Cross-model procedures live under `.agents/skills/`; load a matching `SKILL.md` only on demand. Keep large frontend/admin files and docs section-targeted rather than reading them whole.

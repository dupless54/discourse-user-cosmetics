# AI context system
Use `CURRENT_STATE.md` for the short checkpoint and `WORKFLOW.md` for the shared model roles. For genuine multi-session features add only `docs/ai/work/<feature>/state.md`, `progress.md`, and `implementation-plan.md`; do not store chat transcripts or preload completed phases.
